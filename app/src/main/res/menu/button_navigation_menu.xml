<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/bottom_nav_timeline"
        android:enabled="true"
        android:icon="@drawable/ic_nav_timeline"
        tools:ignore="MenuTitle"
        app:showAsAction="ifRoom"/>

    <item
            android:id="@+id/bottom_nav_top_and_review"
            android:enabled="true"
            android:icon="@drawable/ic_nav_top"
            tools:ignore="MenuTitle"
            app:showAsAction="ifRoom"/>

    <item
            android:id="@+id/bottom_nav_search"
            android:enabled="true"
            android:icon="@drawable/ic_nav_search"
            tools:ignore="MenuTitle"
            app:showAsAction="ifRoom"/>

    <item
            android:id="@+id/bottom_nav_profile"
            android:enabled="true"
            android:icon="@drawable/ic_nav_profile"
            app:showAsAction="ifRoom" tools:ignore="MenuTitle"/>

    <item
            android:id="@+id/bottom_nav_settings"
            android:enabled="true"
            android:icon="@drawable/ic_nav_settings"
            app:showAsAction="ifRoom" tools:ignore="MenuTitle"/>



</menu>